{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "importSorting": {
      "type": "boolean",
      "description": "Whether to enable sorting import statements with a plugin."
    }
  }
}
