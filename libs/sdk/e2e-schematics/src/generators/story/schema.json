{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "Story",
  "title": "Create a new story with a new component and corresponding tests",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What's the name of the component?"
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "x-prompt": "What's the name of the project for which you want to generate the story?"
    },
    "generateCypressSpecs": {
      "type": "boolean",
      "description": "Specifies whether to automatically generate `*.spec.ts` files in the Cypress e2e app generated by the `cypress-configure` generator.",
      "default": true
    },
    "cypressProject": {
      "type": "string",
      "description": "The Cypress project to generate the stories under. This is inferred from `project` by default."
    }
  },
  "additionalProperties": false,
  "required": ["name", "project"]
}
