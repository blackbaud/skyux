{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "ruleset": {
      "enum": ["recommended", "strict-type-checked"],
      "description": "The collection of rules to apply to the ESLint config."
    }
  }
}
