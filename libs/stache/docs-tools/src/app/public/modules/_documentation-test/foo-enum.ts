/**
 * This is the description for FooEnum. It includes `Date` values.
 */
export enum FooEnum {
  /**
   * The bar of the foo.
   */
  Bar = 0,

  /**
   * The baz of the foo.
   */
  Baz,

  /**
   * The `FooUser` birthday as a `Date` object.
   */
  FooUserId
}
