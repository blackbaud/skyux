/**
 * Represents any control panel selections that have changed due to user interaction.
 */
export interface SkyDocsDemoControlPanelChange {
  /**
   * The key/value pair representing the control panel selection that changed.
   */
  [key: string]: any;
}
