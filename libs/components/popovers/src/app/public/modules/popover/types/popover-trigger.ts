/**
 * Specifies the user action that displays the popover.
 * @deprecated Specifying a trigger type other than 'click' will not be supported in the next major version.
 */
export type SkyPopoverTrigger = 'click' | 'mouseenter';
