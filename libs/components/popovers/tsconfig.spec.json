{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "../../../dist/out-tsc",
    "types": ["jasmine", "node"],
    "_comment": "// TODO: Re-enable strict mode for tests after we enable strict mode for core and indicators.",
    "strict": false,
    "noImplicitReturns": false
  },
  "files": ["src/test.ts"],
  "include": ["**/*.spec.ts", "**/*.d.ts"]
}
