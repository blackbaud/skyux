/**
 * A single value plotted by a line chart series: a number renders a point on
 * the line, and `null` renders a gap in the line and an empty cell in the
 * data table.
 *
 * @preview
 */
export type SkyChartLineSeriesValue = number | null;
