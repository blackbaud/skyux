/**
 * The scale type for a chart value axis. Use `logarithmic` when values span
 * several orders of magnitude; otherwise use the default `linear` scale.
 *
 * @preview
 */
export type SkyChartCartesianScaleType = 'linear' | 'logarithmic';
