/**
 * The scale type for a chart measure axis. Use `logarithmic` when values span
 * several orders of magnitude; otherwise use the default `linear` scale.
 *
 * @preview
 */
export type SkyChartMeasureScaleType = 'linear' | 'logarithmic';
