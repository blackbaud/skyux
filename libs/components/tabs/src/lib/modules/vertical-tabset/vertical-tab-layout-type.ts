/**
 * The layout to apply to a vertical tab's content. Layouts apply spacing that
 * corresponds with the top-level component type used within the tab, or
 * constrain the tab contents to the available viewport in the case of `fit`.
 */
export type SkyVerticalTabLayoutType = 'none' | 'blocks' | 'fit' | 'list';
