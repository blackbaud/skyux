import {
  NgModule
} from '@angular/core';

import {
  SkyCoreAdapterService
} from './adapter.service';

@NgModule({
  providers: [
    SkyCoreAdapterService
  ]
})
export class SkyCoreAdapterModule { }
