{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "below-version": {
      "type": "number",
      "description": "Remove the compat stylesheets for SKY UX versions less than this number."
    }
  }
}
