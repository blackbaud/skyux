{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "properties": {
    "version": {
      "type": "string",
      "description": "Remove the compatibility stylesheets at or below the specified version.",
      "default": "current",
      "pattern": "^\\d+|current$"
    }
  }
}
