import { ComponentHarness } from '@angular/cdk/testing';

/**
 * Used by the custom search result template test.
 */
export class ColorIdHarness extends ComponentHarness {
  public static hostSelector = '.my-color-id';
}
