import { SkyQueryableComponentHarness } from '@skyux/core/testing';

/**
 * Harness to interact with the split view workspace footer component in tests.
 */
export class SkySplitViewWorkspaceFooterHarness extends SkyQueryableComponentHarness {
  /**
   * @internal
   */
  public static hostSelector = 'sky-split-view-workspace-footer';
}
