import { SkyQueryableComponentHarness } from '@skyux/core/testing';

/**
 * Harness to interact with the split view workspace content component in tests.
 */
export class SkySplitViewWorkspaceContentHarness extends SkyQueryableComponentHarness {
  /**
   * @internal
   */
  public static hostSelector = 'sky-split-view-workspace-content';
}
