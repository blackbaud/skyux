import { InjectionToken } from '@angular/core';

import { SkyModalHostContextArgs } from './modal-host-context-args';

/**
 * @internal
 */
export const SKY_MODAL_HOST_CONTEXT_ARGS =
  new InjectionToken<SkyModalHostContextArgs>('SkyModalHostContextArgs');
