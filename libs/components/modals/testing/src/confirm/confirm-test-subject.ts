/* eslint-disable @nx/enforce-module-boundaries */
import { SkyConfirmConfig, SkyConfirmInstance } from '@skyux/modals';

export interface SkyConfirmTestSubject {
  config: SkyConfirmConfig;
  instance: SkyConfirmInstance;
}
