/**
 * Arguments used when closing a flyout programmatically.
 */
export interface SkyFlyoutCloseArgs {
  /**
   * Whether the `SkyFlyoutBeforeCloseHandler` should be ignored when closing a flyout.
   */
  ignoreBeforeClose?: boolean;
}
