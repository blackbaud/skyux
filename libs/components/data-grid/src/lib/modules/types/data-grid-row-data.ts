/**
 * The shape of an item passed to the `data` input of `SkyDataGrid`.
 * @preview
 */
export interface SkyDataGridRowData {
  /**
   * A unique identifier for the row.
   * @required
   */
  id: string;
}
