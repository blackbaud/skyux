{
  "name": "skyux",
  "$schema": "node_modules/nx/schemas/project-schema.json",
  "targets": {
    "local-registry": {
      "executor": "@nx/js:verdaccio",
      "options": {
        "port": 4873,
        "config": ".verdaccio/config.yml",
        "storage": "tmp/local-registry/storage"
      }
    },
    "build": {
      "command": "echo '🦾 Overriding the default build target.'",
      "cache": true
    },
    "lint": {
      "command": "echo '🦾 Overriding the default lint target.'",
      "cache": true
    },
    "test": {
      "command": "echo '🦾 Overriding the default test target.'",
      "cache": true
    }
  }
}
