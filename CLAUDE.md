# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Read [AGENTS.md](AGENTS.md) — it is the single source of truth for working in this repo.** It covers the monorepo layout, Nx commands (all/affected/single-project), the Karma vs. Jest test split, public API and dependency discipline, the `sky`/`Sky` export prefix, ESLint layering, formatting, and Conventional Commit rules.

It also points to task-scoped instruction files in [.github/instructions/](.github/instructions/) (Angular, code-example unit tests, harnesses, SCSS overrides) — read the relevant one when your change touches its scope.
